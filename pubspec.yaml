name: demo_app
description: Application de démonstration pour la gestion des mises à jour

publish_to: 'none'
version: 1.0.0+100

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.3.0
  dio: ^5.0.0
  path_provider: ^2.0.11
  open_file: ^3.3.1
  shared_preferences: ^2.0.15
  package_info_plus: ^8.3.0
  flutter_local_notifications: ^19.1.0
  permission_handler: ^12.0.0+1
  flutter_file_dialog: ^3.0.2
  url_launcher: ^6.1.10
  device_info_plus: ^10.0.1 # or latest version

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true